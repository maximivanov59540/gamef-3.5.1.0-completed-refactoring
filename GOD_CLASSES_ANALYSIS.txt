═══════════════════════════════════════════════════════════════════════════════
                      GOD CLASSES - ДЕТАЛЬНЫЙ АНАЛИЗ
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ 1️⃣  BuildingResourceRouting.cs - 1,375 СТРОК
│ 📍  /Construction/Core/BuildingResourceRouting.cs
└─────────────────────────────────────────────────────────────────────────────┘

ОТВЕТСТВЕННОСТИ (6 ГЛАВНЫХ):
  1. ✘ Маршрутизация Output ресурсов
     - Выбор целевого здания для доставки продукции
     - Auto-discovery ближайшего склада (BFS)
     - Методы: RefreshRoutes(), AutoDiscoverOutputDestination()
     
  2. ✘ Маршрутизация Input ресурсов
     - Выбор источника для сырья
     - Auto-discovery (поиск производителя с похожим товаром)
     - Методы: AutoDiscoverInputSource()
     
  3. ✘ Round-Robin распределение
     - Выбор следующего потребителя из списка
     - Счетчик доставок (_deliveryCountToCurrentConsumer)
     - Методы: SelectNextConsumer()
     
  4. ✘ Координация производителей
     - Проверка, занят ли потребитель другим производителем
     - Избежание дублирования доставок
     - Методы: CoordinateWithProducers(), CheckConsumerAllocation()
     
  5. ✘ Управление состоянием маршрутов
     - Кэширование результатов поиска маршрутов
     - Периодическое обновление при изменении сети
     - Поля: outputDestinationTransform, inputSourceTransform
     
  6. ✘ Priority modes
     - preferDirectSupply, preferDirectDelivery
     - enableRoundRobin, enableCoordination
     - Методы выбора стратегии маршрутизации

ЗАВИСИМОСТИ:
  - BuildingRegistry.Instance        (поиск других производителей/потребителей)
  - GridSystem                       (координаты для поиска пути)
  - ResourceCoordinator.Instance     (глобальная координация)
  - RoadManager.Instance             (дорожная сеть, pathfinding)

ПРОБЛЕМЫ:
  ❌ Слишком много конфигурационных полей (12+ параметров в Inspector)
  ❌ Методы RefreshRoutes() содержит O(n²) логику поиска
  ❌ Смешивает логику selection, discovery и coordination
  ❌ 7 публичных методов для слишком разных concerns
  ❌ Интеграция с 4+ синглтонами

РЕКОМЕНДОВАННОЕ РАЗДЕЛЕНИЕ:
  
  ┌─────────────────────────────────────────────────────┐
  │ RoutingResolver (400 строк)                        │
  ├─────────────────────────────────────────────────────┤
  │ public IResourceReceiver ResolveOutputDestination() │
  │ public IResourceProvider ResolveInputSource()       │
  │ private void AutoDiscoverWarehouse()                │
  │ private List<Vector2Int> FindNearestWarehouse()     │
  └─────────────────────────────────────────────────────┘
  
  ┌─────────────────────────────────────────────────────┐
  │ ConsumerSelector (200 строк)                       │
  ├─────────────────────────────────────────────────────┤
  │ public IResourceReceiver SelectNextConsumer()       │
  │ private void RotateConsumerIndex()                  │
  │ private List<IResourceReceiver> GetValidConsumers() │
  └─────────────────────────────────────────────────────┘
  
  ┌─────────────────────────────────────────────────────┐
  │ ProducerCoordinator : Singleton (300 строк)        │
  ├─────────────────────────────────────────────────────┤
  │ public bool IsConsumerAllocated(IResourceReceiver)  │
  │ public void AllocateConsumer(...)                   │
  │ public void ReleaseConsumer(...)                    │
  │ private void CleanupStaleAllocations()              │
  └─────────────────────────────────────────────────────┘
  
  ┌─────────────────────────────────────────────────────┐
  │ BuildingResourceRouting (150 строк - Facade)       │
  ├─────────────────────────────────────────────────────┤
  │ private RoutingResolver _resolver;                  │
  │ private ConsumerSelector _selector;                 │
  │ public void RefreshRoutes() { /* delegate */ }      │
  └─────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│ 2️⃣  BuildingManager.cs - 1,306 СТРОК
│ 📍  /Construction/Core/BuildingManager.cs
└─────────────────────────────────────────────────────────────────────────────┘

ОТВЕТСТВЕННОСТИ (8 ОСНОВНЫХ):
  1. ✘ Building Placement (размещение зданий)
     - EnterBuildMode(), создание ghost building
     - UpdateGhostPosition(), валидация позиции
     - ConfirmBuilding(), размещение на сетке
     - ~200 строк
     
  2. ✘ Building Deletion (удаление зданий)
     - DeleteBuilding(), удаление с сетки
     - Расчет 50% рефунда
     - ~100 строк
     
  3. ✘ Building Movement (перемещение зданий)
     - TryMoveBuilding(), pickup/drop logic
     - UpdateMovePreview()
     - ~150 строк
     
  4. ✘ Building Copying (копирование зданий)
     - TryCopyBuilding(), creation of copy
     - Copy state management
     - ~100 строк
     
  5. ✘ Building Rotation (поворот зданий)
     - RotateGhost(), size swapping
     - Rotation state tracking
     - ~100 строк
     
  6. ✘ Blueprint Mode (режим планирования)
     - HandleBlueprintUpgrade(), convert to real
     - Blueprint cost management
     - ~150 строк
     
  7. ✘ Building Upgrade (улучшение зданий)
     - ExecuteTierUpgrade(), tier progression
     - Tier data management
     - ~200 строк
     
  8. ✘ Ghost Building Management (управление превью)
     - SetupGhostCollider(), создание ghost
     - CancelGhostOnly(), cleanup
     - ~150 строк

ЗАВИСИМОСТИ:
  - ResourceManager.Instance         (проверка ресурсов, траты)
  - MoneyManager.Instance            (проверка золота, траты)
  - EconomyManager.Instance          (проверка долгов)
  - PopulationManager                (для жилых зданий)
  - GridSystem                       (размещение на сетке)
  - PlayerInputController            (смена режимов ввода)
  - NotificationManager              (уведомления игроку)
  - BlueprintManager                 (работа с blueprints)

ПРОБЛЕМЫ:
  ❌ 25 публичных методов (слишком много API)
  ❌ Смешивает валидацию, placement, removal в одном месте
  ❌ Ghost building - это отдельная concern
  ❌ Copy/Move содержат дублированный код
  ❌ Зависит от 8 менеджеров (star-shaped coupling)
  ❌ Трудно тестировать отдельно

РЕКОМЕНДОВАННОЕ РАЗДЕЛЕНИЕ:
  
  ┌──────────────────────────────────────────────┐
  │ BuildingValidator (200 строк)               │
  ├──────────────────────────────────────────────┤
  │ public bool CanPlaceBuilding(...)            │
  │ public bool CanAffordBuilding(...)           │
  │ public bool IsGridCellFree(...)              │
  │ public (bool, string) ValidatePlacement()    │
  └──────────────────────────────────────────────┘
  
  ┌──────────────────────────────────────────────┐
  │ BuildingPlacer (250 строк)                  │
  ├──────────────────────────────────────────────┤
  │ private BuildingValidator _validator;        │
  │ public void EnterBuildMode(...)              │
  │ public void UpdateGhostPosition(...)         │
  │ public GameObject ConfirmBuilding()          │
  │ private void CreateGhostPreview(...)         │
  └──────────────────────────────────────────────┘
  
  ┌──────────────────────────────────────────────┐
  │ BuildingRemover (200 строк)                 │
  ├──────────────────────────────────────────────┤
  │ private const float REFUND_PERCENTAGE = 0.5f │
  │ public void DeleteBuilding(...)              │
  │ private float CalculateRefund(...)           │
  │ private void ReturnResources(...)            │
  └──────────────────────────────────────────────┘
  
  ┌──────────────────────────────────────────────┐
  │ BuildingTransformer (300 строк)             │
  ├──────────────────────────────────────────────┤
  │ public void MoveBuilding(...)                │
  │ public void RotateBuilding(...)              │
  │ public void CopyBuilding(...)                │
  │ public void UpgradeBuilding(...)             │
  │ private void HandleBlueprintUpgrade(...)     │
  │ private bool ExecuteTierUpgrade(...)         │
  └──────────────────────────────────────────────┘
  
  ┌──────────────────────────────────────────────┐
  │ BuildingManager : Facade (300 строк)        │
  ├──────────────────────────────────────────────┤
  │ private BuildingPlacer _placer;              │
  │ private BuildingRemover _remover;            │
  │ private BuildingTransformer _transformer;    │
  │ // Delegates to specialized classes         │
  └──────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│ 3️⃣  CartAgent.cs - 1,262 СТРОК
│ 📍  /Economy/Warehouse/CartAgent.cs
└─────────────────────────────────────────────────────────────────────────────┘

ОТВЕТСТВЕННОСТИ (7 ОСНОВНЫХ):
  1. ✘ State Machine (управление состояниями)
     - 6 состояний: Idle → Loading → Delivering → Unloading → ... → Returning
     - Логика переходов между состояниями
     - ~400 строк
     
  2. ✘ Cargo Slot Management (управление грузовыми слотами)
     - 3 слота по 5 единиц каждый
     - Проверка вместимости
     - Добавление/удаление ресурсов
     - ~200 строк
     
  3. ✘ Movement (движение по карте)
     - Pathfinding (BFS на дорожной сети)
     - Vector3 интерполяция позиции
     - Следование пути
     - ~250 строк
     
  4. ✘ Loading/Unloading (загрузка/разгрузка)
     - Корутины для загрузки продукции
     - Синхронизация с BuildingOutputInventory
     - Синхронизация с BuildingInputInventory
     - ~200 строк
     
  5. ✘ Resource Requests (запросы ресурсов)
     - Запрос к BuildingResourceRouting
     - Выбор следующего здания-получателя
     - Обработка отказов
     - ~150 строк
     
  6. ✘ Error Handling (обработка ошибок)
     - Stuck detection (если путь не найден)
     - Cleanup при недостижимости
     - Fallback стратегии
     - ~100 строк
     
  7. ✘ Home Base Management (управление базой)
     - Хранение ссылки на дом
     - Получение inventory компонентов
     - Возврат домой
     - ~100 строк

ЗАВИСИМОСТИ:
  - GridSystem                       (координаты, pathfinding)
  - RoadManager.Instance             (дорожная сеть для пути)
  - BuildingRegistry.Instance        (поиск зданий по типам)
  - BuildingResourceRouting          (выбор маршрутов)
  - BuildingOutputInventory          (получение Output)
  - BuildingInputInventory           (доставка Input)

ПРОБЛЕМЫ:
  ❌ State machine занимает 400+ строк в одном методе Update()
  ❌ Cargo slots встроены в класс (должны быть отдельным компонентом)
  ❌ Movement логика смешана с pathfinding
  ❌ Сложный жизненный цикл корутин
  ❌ Трудно отладить (много зависимостей)
  ❌ GetComponent вызовы в начале work cycle

РЕКОМЕНДОВАННОЕ РАЗДЕЛЕНИЕ:
  
  ┌──────────────────────────────────────────────┐
  │ CartInventory (150 строк)                   │
  ├──────────────────────────────────────────────┤
  │ [System.Serializable]                        │
  │ private CargoSlot[] _cargoSlots = new [3];   │
  │ public bool TryAddResource(...)              │
  │ public bool TryRemoveResource(...)           │
  │ public float GetTotalWeight()                │
  │ public bool HasSpace(ResourceType)           │
  └──────────────────────────────────────────────┘
  
  ┌──────────────────────────────────────────────┐
  │ CartPathfinder (150 строк)                  │
  ├──────────────────────────────────────────────┤
  │ public bool TryFindPath(...)                 │
  │ public void FollowPath(List<Vector2Int>)     │
  │ private void HandlePathfindingFailure()      │
  │ public bool IsPathValid()                    │
  └──────────────────────────────────────────────┘
  
  ┌──────────────────────────────────────────────┐
  │ CartMovement (250 строк)                    │
  ├──────────────────────────────────────────────┤
  │ private CartPathfinder _pathfinder;          │
  │ public void MoveToTarget(Vector2Int)         │
  │ public void FollowPath(...)                  │
  │ private void UpdatePosition()                │
  │ public bool HasReachedTarget()               │
  │ [SerializeField] public float moveSpeed      │
  └──────────────────────────────────────────────┘
  
  ┌──────────────────────────────────────────────┐
  │ CartAgent : State Machine (300 строк)       │
  ├──────────────────────────────────────────────┤
  │ private CartMovement _movement;              │
  │ private CartInventory _inventory;            │
  │ private enum State { Idle, Loading, ... }    │
  │ public void Update() { /* state logic */ }   │
  │ private void TransitionTo(State newState)    │
  │ // High-level orchestration только           │
  └──────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════

ОСТАЛЬНЫЕ GOD CLASSES (500-700 строк):

4️⃣  GroupOperationHandler.cs - 620 СТРОК
    Разделить на: GroupSelector (selection) + GroupExecutor (batch ops)
    
5️⃣  RoadCoverageVisualizer.cs - 564 СТРОК
    Разделить на: CoverageCalculator (логика) + CoverageRenderer (визуализация)

═══════════════════════════════════════════════════════════════════════════════
